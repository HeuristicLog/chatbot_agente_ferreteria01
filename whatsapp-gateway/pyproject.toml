[project]
name = "whatsapp-gateway"
version = "0.1.0"
description = "Gateway de mensajeria desacoplado para WhatsApp"
dependencies = [
    "fastapi>=0.100.0",
    "uvicorn[standard]>=0.22.0",
    "pydantic>=2.0.0",
    "httpx>=0.24.1",
    "redis>=4.6.0"
]

[tool.setuptools]
packages = ["app"]
